node_modules
.env

# Cursor MCP config (may contain API keys / Bearer tokens)
.cursor/mcp.json
~/*

# Hardhat files
/cache
/artifacts

# TypeChain files
/typechain
/typechain-types

# solidity-coverage files
/coverage
/coverage.json

# Hardhat Ignition default folder for deployments against a local node
ignition/deployments/chain-31337

# Cached ETH/USD price for production deploys (see getEthUsdPrice in
# DeployAndConfig.ts) -- machine/time-specific, never meant to be committed
ignition/.eth-price-cache.json

# Large build info files
ignition/deployments/chain-545/build-info/*
ignition/deployments/chain-84532/build-info/*

# Photoshop
.psd
*.psd
